# pytorch-learning

I'm learning PyTorch! I started by making linear regression models, then classification, then convolutional. I tried to make it more experiencable by adding a linear regression explorer to show how hyperparameters affect training and a data augmentation explorer to learn how data augmentation changes input data. I did this so the project can be experiencable while I'm still learning how to make a fully fledged out model.
